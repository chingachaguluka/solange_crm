    <!-- display error on page if any -->
    <% if @contact.errors.any? %>
        <div class="alert alert-danger alert-dismissable">
            <p>Please correct the following <%= pluralize(@contact.errors.count, "error") %>:</p>
            <ul>
                <% @contact.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Edit Contact</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-9 col-md-9">
          <div class="panel panel-primary">
            <!-- /.panel-heading -->
            <div class="panel-heading">
                Edit Contact Details
            </div>

            <%= render 'form' %>

            </div>
          <!-- /.panel -->
          </div>
        <!-- /.col-lg-9 -->
        <div class="col-lg-3 col-md-3">
            <div class="panel panel-primary">
            <!-- /.panel-heading -->
            <div class="panel-body">
                <p><%= link_to "All Contacts", contacts_path %></p>
                <p><%= link_to "New Contact", new_contact_path %></p>
                <p><%= link_to "Delete Contact", @contact, method: :delete, data: { confirm: "You sure?" } %></p>
            </div>
            <!-- /.panel-body -->        
          </div>
            <!-- /.panel -->
        </div>
        <!-- end div col-lg-3 -->
    </div>
    <!-- /.row -->
  </div>